{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft.AspNetCore": "Information"
      }
    }
  },
  "Auth": {
    "AllowedOrigins": [ "http://localhost:5173", "https://localhost:5173" ],
    "//RefreshCookieSameSiteStrict": "Kept strict locally too — the SPA proxies /api through Vite, so it is same-site in development as well.",
    "RefreshCookieSameSiteStrict": true
  }
}
